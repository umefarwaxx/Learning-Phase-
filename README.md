# Learning-Phase
This is my first Git Repository
<br>
<h2> Terminal Commands </h2> <br>
1) pwd         -->  This command shows current location (Confirm Correct Folder)<br>
2) cd          -->  This command use for change directory<br>
3) ls          --> This command shows list of files/folders<br>
4) ls -a       --> all list of files/folders (include hidden) < -- >(confirm .git exists  If not then initialize --> git init)<br>
5) git status  -->  This command Confirm Git is active<br>
6) ls -a       --> confirm .git exists<br>
7) git config user.name "abc"<br>
8) git config user.email "abc@gmail.com"<br>
9) git config --list --show-origin -->  confirm local config<br>
10) git config --global   -->  This command use for global<br><br>
11) git clone  link---    --> cloning a repo on our local machine<br>
12) git status   -->  display code status<br>
<ls><h4>we have foure types of status</h4>
  <li> Untracked </li>
  <li> Modified </li>
  <li> Staged </li>
  <li> Unmodified </li></ls>
<br>
<h2>Iint Command</h2>
<ol> <li>git init</li> 
 <li>git add (file name) or .</li> 
 <li>git commit -m 'meaningful msg'</li> 
 <li>git branch    (to check branch) </li> 
 <li>git branch -M main (rename) </li> 
 <li>git remote add origin https://github.com/project-name/localrepo.git</li>
 <li>git remote -v (to verify remote) </li> 
 <li>git push -u origin main</li> </ol>

(16-Feb-26)
<h2> Branch Command</h2>
<ol> <li>git branch</li> 
 <li>git branch -M main (to rename)</li> 
 <li>git checkout branch name (to navigate)</li> 
 <li>git checkout -b new branch name    (to check new branch) </li> 
 <li>git branch -d branch name (to delete branch) </li> 
 

</ol>


